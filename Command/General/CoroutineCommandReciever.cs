using System.Collections.Generic;
using System;
using UnityEngine;
using UTIRLib.Diagnostics;

namespace UTIRLib.CommandSystem
{
#nullable enable
    //public sealed class CoroutineCommandReciever : MonoBehaviourExtended, ICommandReciever<ICoroutineCommand>
    //{
    //    private readonly List<ICoroutineCommand> commands;
    //    private int commandsCount;

    //    public void AddCommand(ICoroutineCommand? command)
    //    {
    //        if (command == null) {
    //            Debug.LogWarning(new ArgumentNullMessage(nameof(command)));
    //            return;
    //        }

    //        commands.Add(command);
    //        commandsCount = commands.Count;
    //    }

    //    private void Update()
    //    {
    //        for (int i = 0; i < commandsCount; i++) {
    //            commands[i].
    //        }
    //    }
    //}
}
